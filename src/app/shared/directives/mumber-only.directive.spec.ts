import { MumberOnlyDirective } from './mumber-only.directive';

describe('MumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new MumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
